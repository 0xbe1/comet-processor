{
  "name": "@sentio/processor-template",
  "version": "1.0.0",
  "scripts": {
    "test": "jest",
    "build": "sentio build",
    "upload": "sentio upload",
    "postinstall": "sentio gen"
  },
  "dependencies": {
    "@sentio/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/jest": "^28.0.0",
    "jest": "^28.0.1",
    "ts-jest": "^28.0.8",
    "typescript": "^4.7.2"
  }
}
